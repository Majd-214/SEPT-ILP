# Dependencies
node_modules/

# Build output
dist/

# Platform local state: SQLite database, releases, and the mail sink.
# All of it is reproducible — content lives in git, releases are built
# from it, and accounts are recreated by `npm run seed`.
data/
apps/platform/.data/

# Environment and editor artifacts
.env
.DS_Store
*.log
