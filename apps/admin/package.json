{
  "name": "sept-ilp-admin",
  "version": "1.0.0",
  "description": "Schema-driven content editor mounted by the platform: drafts outside git, AJV validation with the renderer's own schemas before anything is committed, git commits authored by the signed-in editor, image uploads with mandatory alt text. See docs/decisions/adr-001-cms.md.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@fastify/multipart": "^9.0.3",
    "@fastify/static": "^8.1.1",
    "fastify": "^5.2.1"
  }
}
